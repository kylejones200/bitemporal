pandas>=1.5
numpy>=1.23
requests>=2.28
matplotlib>=3.6
duckdb>=0.8             # bitemporal_duckdb.py: vectorised as-of joins at scale
pandas_datareader>=0.10 # cleanest pull of the current FRED series (web.DataReader)
# optional, for the test runner if you prefer pytest over `python tests/test_*.py`
# pytest>=7.0
